# khaiminov.com — спека и контекст проекта

Живой документ. Ведём его в репозитории, чтобы контекст переживал переезд
между компами/аккаунтами. Правь свободно, секции ниже — не догма.

## Идея

Личный сайт (карьера + человек за ней). Метафора: **стена с гирляндой
тёплого света**, под гирляндой на стене — фотографии (на скотче или висят
на нитке/прищепке). Главная — пять "фото" на стене: Карьера, Проекты, Блог,
Личное, Контакты. Переход в раздел — та же стена, но уже с фото по месту
работы/теме. Клик по фото — экран "подробнее" (стори): несколько фото +
текст истории.

Цветовая гамма не жёстко закреплена (стена — не обязательно фиолетовая),
но за основу взяли текущий бренд-акцент (тёмный сливовый фон + тёплая
бронза/золото гирлянды) — можно менять.

## Технические решения

- **Стек:** Astro + GitHub Actions (build) → деплой на Cloudflare Pages
  (точнее — Cloudflare Workers Builds, unified-продукт; см. `wrangler.jsonc`
  и раздел "Cloudflare" ниже). Статический вывод, без бэкенда/API. Даёт из
  коробки генерацию `/ru/` и `/en/` из одного шаблона.
  - Для фазы мокапа (эта ветка) — пишем как plain HTML/CSS/JS, без сборки.
    Перенос в Astro-структуру — после утверждения макета и наполнения
    контентом.
  - **Структура репо:** сайт живёт в `/apps/site` (`index.html`,
    `style.css`, `app.js`, `assets/`, `version.json`, `_headers`).
    `wrangler.jsonc` остаётся в корне репо (там его ищет Cloudflare
    Workers Builds) и просто указывает `assets.directory: "./apps/site"`.
    Блог — отдельно, `/apps/blog`, появится и будет деплоиться отдельно
    позже (не блокирует эту ветку). `docs/` — не деплоится, вне
    `assets.directory`, живёт в корне.
- **Cloudflare — как реально настроено (Workers Builds):**
  - Git-репозиторий подключён напрямую (без своего `.github/workflows`,
    билдит сам Cloudflare).
  - **Deploy command** (для продакшен-ветки `main`): `npx wrangler deploy`
    — сразу загружает и переключает трафик.
  - **Version command** (для всех остальных веток, "Builds for
    non-production branches" включена): `npx wrangler versions upload` —
    только загружает версию с отдельным preview-URL, `khaiminov.com` не
    трогает. Это осознанный дизайн Cloudflare, не баг — обе команды разные
    и настроены правильно.
  - Каждый non-production билд даёт две ссылки в логе: Version Preview URL
    (уникальный по ID версии) и Version Preview Alias URL вида
    `https://<ветка>-khaiminov-com.<account>.workers.dev` — по алиасу
    удобнее всего смотреть текущий стейт конкретной ветки.
  - `wrangler.jsonc` в корне репо обязателен — без него билд падает с
    "Missing entry-point to Worker script or to assets directory".
    `assets.directory` указывает на `./apps/site` (см. выше).
- **Локализация (ru/en):** один шаблон + словари переводов (JSON/данные).
  В мокапе — переключатель ru/en на клиенте поверх объекта с контентом
  (`apps/site/app.js`), финальная версия — через Astro i18n routing.
  Тексты частично переводит Клод, но должна быть возможность править
  вручную (просто текстовый файл — легко).
- **Фото:** сжимаются заранее (resize + WebP) и коммитятся уже лёгкими.
  Автоматизацию через Actions пока не делаем — сложность не нужна при
  таких объёмах.
- **Хостинг:** Cloudflare Pages, бесплатный тариф (уже настроен на
  `khaiminov.com`, см. `_headers` в корне).
- **Блог:** отдельный репозиторий, поддомен `blog.khaiminov.com`. С
  главного сайта — просто ссылка (карточка на стене), без интеграции
  контента.
- **Почта на `khaiminov.com`:** отдельная задача, не блокирует мокап.
  На момент разговора похоже, что Cloudflare free tier не даёт email
  routing/hosting в нужном виде — надо перепроверить и настроить отдельно.
- **CV:** будет лежать в разделе "Контакты" (кнопка скачивания). Файл
  `cv.pdf` пока не в репозитории — возьмём финальную версию у Алексея
  (есть `Alexey_Khaiminov_CV.pdf` / `Khaiminov_Resume_RU.pdf` на его
  Desktop, но это черновики для генерации сайта, не обязательно то, что
  ляжет на сайт как есть).

## Layout

Двухколоночная схема на каждой странице:
- **Слева** — постоянное меню (Карьера / Проекты / Блог / Личное /
  Контакты), подсвечивает активный раздел.
- **Справа** — "стена" с гирляндой сверху и фото-карточками
  (полароид на нитке/скотче).
- **Синхронная подсветка**: на главной наведение на пункт меню подсвечивает
  соответствующую карточку на стене и наоборот (`data-sync` + классы
  `.is-synced` в `app.js`). Внутри разделов (Карьера и т.д.) меню слева
  просто показывает активный верхнеуровневый раздел, без дублирования
  списка компаний/проектов слева — второй список показался бы избыточным.
- **Клик по карточке** → оверлей "стори" (несколько фото + текст). Работает
  универсально для Карьеры/Проектов/Личного через одну функцию
  `openStory(collection, id)`.
- **Мобильная адаптация**: на ширинах ≤860px левое меню превращается в
  горизontальную прокручиваемую полосу под шапкой (без гамбургера — проще и
  надёжнее). Карточки перетекают в 1 колонку на самых узких экранах.
- **Иллюстрация стены/гирлянды**: под рукой нет инструмента генерации
  изображений (DALL-E и т.п.), поэтому стена и гирлянда — это проработанный
  SVG/CSS (шум на стене через `feTurbulence`, лампочки — радиальные
  градиенты с двойным свечением и мерцанием). Если понадобится
  "настоящая" сгенерированная картинка — нужно принести файл извне и
  вставить как фон.

## Структура разделов

Главная стена (5 карточек): **Карьера · Проекты · Блог · Личное · Контакты**

### Карьера (реальные данные из резюме — `Alexey_Khaiminov_CV.pdf`)

Хронологический таймлайн, каждая "работа" = фото-карточка на стене
раздела, клик → стори с деталями:

1. **Казахстан — портлеты под WebSphere** — ⚠️ нет данных (компания,
   даты, роль). Уточнить у Алексея.
2. **BSS** · Android-разработчик · июн 2013 – апр 2015 · Москва (2 года)
   Портфель из 26 банковских Android-приложений.
3. **Mail.Ru Игры** · апр–ноя 2015 · Москва. Два проекта под NDA
   (маленькая карточка, без подробностей).
4. **Одноклассники (OK.RU)** · ноя 2015 – апр 2018 · Москва (в составе
   Mail.Ru Group). ~40 млн MAU. Флоу входа, регистрации, восстановления
   аккаунта.
5. **Юла** · 2018–2021 · Москва (Principal Android Engineer, Mail.Ru
   Group). ~1,8 млн DAU / ~11,5 млн MAU, crash-free 99,9%. Clean+MVI
   рефакторинг, GraphQL-миграция (6→1 сетевых запросов при старте),
   мультистор-дистрибуция (Google Play/Huawei/RuStore).
6. **ВКонтакте** · Android Platform Lead, Marketplace BU · дек 2021 – мар
   2023 · Москва (1 год 3 мес). Лид ~20 разработчиков (VK Маркет + Юла).
   Холодный старт Юлы −55%, «Инициативные группы» (техдолг-программа),
   доклады на Mobius/Heisenbug, преподавание в Сириусе/Техносфере ВК.
7. **Plata-банк** · Senior/Staff Android Engineer — Platform · дек 2022 –
   н.в. · Лимасол, Кипр. Медианное время сборки −35%, миграция DI
   Dagger→Metro (холодная сборка ×1.5), Baseline Profiles (холодный старт
   ~20%), Mobile Toolbox (внутренняя телеметрия).

### Проекты

Фриланс-проекты — список будет пополняться позже. В мокапе — заглушки.

### Блог

Просто внешняя ссылка-карточка на `blog.khaiminov.com` (отдельный репо).

### Личное

Увлечения вне программирования: гитара, ножи, менторство в «Творчестве
чувств». В мокапе — заглушки-карточки, содержание позже.

### Контакты

Из резюме:
- Email: `alex.haiminov@gmail.com`
- Telegram: `@SmilingZen`
- GitHub: `github.com/xvar`
- Локация: Лимасол, Кипр (готов к релокации)
- LinkedIn — в резюме есть ссылка "Public Talks (LinkedIn)", но точный
  URL профиля не восстановлен из PDF (текст ссылки виден, href — нет).
  Нужно уточнить у Алексея финальный список контактов для сайта (email
  на домене? phone? другое?).
- Кнопка "Скачать CV" — уже подключена к реальным файлам
  `assets/Alexey_Khaiminov_CV_EN.pdf` / `assets/Alexey_Khaiminov_CV_RU.pdf`
  (черновики с Desktop Алексея), переключается вместе с языком интерфейса.
  Не факт, что это финальная версия резюме для сайта — заменить, когда
  будет готов вариант именно "для сайта".

## Открытые вопросы (нужен ответ Алексея)

- [ ] Казахстан/WebSphere: компания, даты, роль, 1-2 достижения.
- [ ] Финальный список контактов для сайта (какие каналы показывать).
- [ ] LinkedIn URL.
- [ ] Список фриланс-проектов (когда будут готовы).
- [ ] Детали по разделу "Личное" (что конкретно показывать по гитаре/
      ножам/менторству — фото, короткие тексты, ссылки?).
- [ ] Финальный `cv.pdf` для раздела контактов.
- [ ] Настройка почты на `khaiminov.com` через Cloudflare (после мокапа).
- [ ] Реальные фото по разделам (сейчас в мокапе — плейсхолдеры).

## Статус

- Ветка `redesign/mockup` — визуальный макет, живёт в `/apps/site` (см.
  "Структура репо" выше; URL не меняется, деплоится тот же
  `assets.directory`). Реализовано: двухколоночный
  layout (меню слева / стена справа) с синхронной подсветкой, кликабельные
  карточки во всех разделах со "стори"-оверлеем, реальные данные карьеры и
  контакты/CV из резюме, RU/EN, мобильная адаптация, футер с версией/коммитом.
  Фото везде ещё плейсхолдеры (эмодзи на градиенте).
